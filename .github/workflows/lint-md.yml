name: lint markdown

on: [push, pull_request]

jobs:
  lint-markdown:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v7

      - name: lint-md-github-action
        uses: lint-md/github-action@v0.2.0
        with:
          configFile: .lintmdrc
          failOnWarnings: false
          files: ./*.md ./docs/*.md ./docs/*/*.md ./docs/*/*/*.md ./src/*/*.md